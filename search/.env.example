# Only used to build the `search-secrets` Modal secret (see README.md) — the
# agent's own containers never read a .env file directly. Same value as
# database/.env's SUPABASE_DB_URL — deployed as its own secret so this
# feature's containers don't also get MAL/Sheets credentials they don't need.
SUPABASE_DB_URL=
